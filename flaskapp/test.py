from packages.tel_scraper import TelSearch
from packages.tel_scraper import LocalCH

from config import info_page_file

query = {
    'was': 'Margot Widmer',
    'wo': 'Seebach',
    'category': ''
}

# df = LocalCH.page_aggregator(query)
# df = TelSearch.page_aggregator(query)
# print(df)
